// Low-conversion badge thresholds (C-S5): an agent row is flagged when its
// offerRate is below LOW_CONVERSION_OFFER_RATE with at least
// LOW_CONVERSION_MIN_SUBMITTED submitted applications (small samples are skipped).
export const LOW_CONVERSION_MIN_SUBMITTED = 5;
export const LOW_CONVERSION_OFFER_RATE = 0.25;

export const DIRECT_ROW_KEY = 'direct';
